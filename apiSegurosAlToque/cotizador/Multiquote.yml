info:
  name: Multiquote
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/v1/quotes/auto/multi
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
